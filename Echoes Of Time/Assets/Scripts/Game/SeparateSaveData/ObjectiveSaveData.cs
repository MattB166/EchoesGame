using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class ObjectiveSaveData 
{
    //save all the previously achieved objectives
    //this is used to prevent the player from achieving the same objective multiple times
    //save all the current objectives, so we pick up where we left off. 
}
