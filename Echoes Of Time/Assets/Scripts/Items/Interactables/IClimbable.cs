using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// Interface denoting all climbable objects. used for searching for climbable objects, no methods required. 
/// </summary>
public class IClimbable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
